concourse:
  worker:
    autoscaling:
      maxReplicas: 1
      buildInMetrics:
        - type: Resource
          resource:
            name: cpu
            target:
              type: Utilization
              averageUtilization: 80
